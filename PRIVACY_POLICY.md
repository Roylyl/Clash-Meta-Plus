# 数据处理与隐私说明

适用对象：本仓库的 Clash Meta Plus 17.01 源代码。核查日期：2026-09-13。

本文根据本地 Android 应用代码、直接依赖声明及配置文件说明可核实的数据处理路径。它不是对所有第三方 SDK、Go 依赖、设备系统服务或配置所连接服务器的完整隐私审计，也不承诺任何构建都不存在其他网络行为。修改依赖、配置或分发方式后，应重新核对相应说明。

## 配置与网络连接

导入的配置、订阅信息、代理选择和应用设置保存在应用使用的本地文件、数据库或偏好存储中。实现线索包括 `service/.../data/Database.kt` 和 `ProfileProcessor.kt`。这些内容可能包含订阅令牌、节点凭据和服务器地址。

导入或更新远程配置时，应用会访问配置来源；启用定时更新时，也会按相应计划再次请求。VPN/代理运行期间，流量、DNS 查询及规则中要求的远程资源请求由配置和内核处理。相应的订阅、代理、DNS 或资源服务器可能接收 IP 地址、请求内容及协议所需数据，其处理方式取决于该服务器和配置。客户端源码不等同于这些服务提供者的隐私政策。

构建阶段会从 meta-rules-dat 下载 Geo 数据并打包；`MainApplication.kt` 在设备上将已打包的四个文件复制到应用目录。配置或功能所触发的后续数据、规则更新另由实际配置和内核行为决定。资源来源见 [THIRD_PARTY.md](THIRD_PARTY.md)。

## 应用列表、扫码与文件访问

分应用访问控制读取已安装应用的包信息；`AccessControlActivity.kt` 和 `AppListCacheModule.kt` 使用这些信息展示选择列表并建立应用与流量归属映射。应用声明了查询应用列表的权限。

扫码导入通过 `NewProfileActivity.kt` 调用 Quickie。其相机与识别功能包含第三方依赖，本次未对这些依赖的全部行为进行动态审计。导入、导出文件通过相应的 Android 文件选择或文档接口完成；选择云端文档提供者时，还可能涉及该提供者的数据处理规则。

## 日志与导出

日志功能可将时间、级别及运行消息写入本地日志文件，参见 `LogcatWriter.kt`。`LogcatActivity.kt` 提供查看、删除及通过系统文档接口导出功能。运行消息可能包含域名、IP 地址、配置名称或错误上下文；导出或反馈前应自行检查和脱敏。

本次在应用源码、直接 Gradle 依赖声明和初始化代码中未发现 AppCenter SDK 的声明或初始化调用。因此没有继续使用旧文档中“通过 AppCenter 收集错误日志”的现行事实表述。这个结论仅覆盖所述检查范围，不等于证明全部传递依赖或全部网络流量均无数据上报。

## 系统备份与用户控制

当前 `app/src/main/AndroidManifest.xml` 设置 `android:allowBackup="true"`。`app/src/main/res/xml/full_backup_content.xml` 将偏好设置、数据库、导入/待处理配置及 `clash/override.json` 纳入其备份规则。因此，不能将“本地存储”理解为数据绝不离开设备；实际云备份或设备迁移行为还取决于 Android 版本、厂商实现和用户的系统设置。

你可以在应用内删除配置和日志，通过 Android 应用设置清除应用数据，并按需要管理系统备份。清除应用数据不会自动删除此前导出的文件、系统备份副本，或第三方服务器已接收的信息；这些副本需在各自位置处理。

## 外部链接与分发者说明

帮助页面中的链接会交由浏览器等外部应用打开，对应网站适用其自身规则。本文没有将上游作者、MetaCubeX 或任何第三方公司指定为本分支的运营者，也没有编造服务器、联系方式、保留期限或数据删除承诺。

若将本分支作为产品对外分发，实际分发者应按真实构建、服务接入和运营情况补充必要的身份、联系和隐私信息。原源码归档中的历史模板保存在 [`docs/upstream/PRIVACY_POLICY.original.md`](docs/upstream/PRIVACY_POLICY.original.md)，仅供追溯，不作为本分支当前的隐私政策。
